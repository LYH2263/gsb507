# 图书借阅并发问题分析报告

## 涉及源文件

- [BorrowService.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java) → `BorrowService.borrowBook`
- [BorrowRecordMapper.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java) → `BorrowRecordMapper.countActiveBorrowsByBookId`、`BorrowRecordMapper.insert`

---

## 一、并发时序交错分析

`alice` 与 `bob` 几乎同一毫秒对 `bookId = 5` 发起借阅，两个请求都会进入 [BorrowService.borrowBook](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L55-L81)。由于 `@Transactional` 默认隔离级别走 MySQL InnoDB 的 **REPEATABLE READ**，且 [countActiveBorrowsByBookId](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L39-L40) 是一条不带 `FOR UPDATE` 的普通 `SELECT COUNT(*)`，最坏情况下时序会是：

| 时刻 | Alice 事务 (T1) | Bob 事务 (T2) |
| ---- | --- | --- |
| t1 | BEGIN | |
| t2 | `countActiveBorrowsByBookId(5)` 返回 0（快照读） | |
| t3 |  | BEGIN |
| t4 |  | `countActiveBorrowsByBookId(5)` 返回 0（自己的快照中也看不到 T1 未提交的插入） |
| t5 | `insert` 写入 (alice, 5, 'BORROWED') | |
| t6 |  | `insert` 写入 (bob, 5, 'BORROWED') |
| t7 | COMMIT | |
| t8 |  | COMMIT |

**最终结果**：`sys_borrow_record` 中会落下 **2 条** `status = 'BORROWED'` 的记录，分别属于 alice 和 bob，"单本馆藏"约束被打破。

问题根因位于 [BorrowService.borrowBook](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L70-L80) 的"先 count 再 insert"两步操作之间，存在一个**校验—写入时间窗口（Check-Then-Act / TOCTOU）**。

---

## 二、`@Transactional` 是否足以守住"单本馆藏"

**不能。** 原因有三：

### 1. REPEATABLE READ 不阻止其他事务插入

InnoDB 的 RR 隔离级别只保证事务内多次**普通快照读**结果一致，并不会对 [countActiveBorrowsByBookId](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L39-L40) 扫描的范围加任何锁。两个事务可以同时读到 `count = 0`，再同时插入，互不感知。

### 2. 普通 SELECT 是"快照读"，看不见对方未提交的数据

即使 T2 的 `count` 在 T1 已经 `INSERT` 之后执行，由于 RR 下的一致性视图（read view）在事务首条语句时建立，T2 仍然读到 0。再加上 T1 未提交时本来也不可见，T2 必然误判"无人借阅"。

### 3. `sys_borrow_record` 没有任何唯一约束兜底

[BorrowRecordMapper.insert](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L18-L19) 直接写入，数据库层面没有 `UNIQUE(book_id, status='BORROWED')` 之类的约束，两条 `INSERT` 都会成功。

**结论**：`@Transactional` 只能保证 [BorrowService.borrowBook](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L55-L81) 内多步操作的"原子提交/回滚"，并不能将"校验 + 写入"序列化。要消除时间窗口，必须显式加锁或借助数据库唯一约束。

---

## 三、两种修复思路与取舍

### 方案 A：悲观锁 —— 在校验前对图书行加 `SELECT ... FOR UPDATE`

在 [BorrowService.borrowBook](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L55-L81) 中，在调用 [countActiveBorrowsByBookId](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L39-L40) 之前，先对 `sys_book` 中 `id = bookId` 这一行执行 `SELECT ... FOR UPDATE`（或在 `BorrowRecordMapper` 中新增一条带 `FOR UPDATE` 的活动借阅查询），把图书行级锁握在事务里直到 `COMMIT`。

```sql
-- 新增 mapper 方法
SELECT id FROM sys_book WHERE id = #{bookId} FOR UPDATE
```

- **优点**：逻辑直观，事务内"加锁—校验—写入—提交"完整闭环；阻塞而非失败，对调用方无感。
- **缺点**：依赖数据库行锁，并发量大时可能引发锁等待、死锁；事务必须尽量短小；跨多个数据库分片会失效。

### 方案 B：数据库唯一约束 —— 用唯一索引 + 插入冲突来兜底

在 `sys_borrow_record` 上建立一个仅对 "BORROWED" 状态生效的唯一索引，让数据库本身阻止"同一本书出现两条 BORROWED"。MySQL 没有部分索引，常见做法是引入辅助列：

```sql
ALTER TABLE sys_borrow_record ADD COLUMN active_book_id BIGINT
  GENERATED ALWAYS AS (CASE WHEN status = 'BORROWED' THEN book_id END) VIRTUAL;
ALTER TABLE sys_borrow_record ADD UNIQUE KEY uk_active_book (active_book_id);
```

之后 [BorrowRecordMapper.insert](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L18-L19) 在并发冲突时会抛 `DuplicateKeyException`，[BorrowService.borrowBook](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L55-L81) 捕获后转为友好提示即可。

- **优点**：约束下沉到存储层，任何路径（接口、脚本、后台批处理）都绕不过去；无锁等待，吞吐高。
- **缺点**：需要变更表结构（加生成列 / 辅助列）；归还时需要把 `book_id` 同步置空或保持 `status` 切换语义；业务层要处理冲突异常并给出合理 UX；历史数据需迁移修复。

### 取舍建议

- 若并发量不高、追求改动最小，先采用 **方案 A（行级悲观锁）**。
- 若希望长期稳定、防御所有写入路径，应叠加 **方案 B（唯一约束）** 作为最终兜底；两者不互斥，生产环境推荐"应用层加锁 + 数据库唯一约束"双保险。

---

## 四、问题清单（按源文件 / 方法定位）

| # | 位置 | 问题 |
| - | ---- | ---- |
| 1 | [BorrowService.borrowBook](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L70-L80) | "校验—写入"两步之间存在 TOCTOU 时间窗口，并发下会落下多条 BORROWED 记录 |
| 2 | [BorrowRecordMapper.countActiveBorrowsByBookId](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L39-L40) | 普通快照读，未带 `FOR UPDATE`，无法阻止其他事务并发插入 |
| 3 | [BorrowRecordMapper.insert](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L18-L19) | 直接 INSERT，且 `sys_borrow_record` 无唯一约束兜底，重复借阅不会被数据库拦截 |
| 4 | [BorrowService.borrowBook](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L55) 上的 `@Transactional` | 仅保证原子提交，不保证序列化；REPEATABLE READ 不能将"先查后插"串行化 |
