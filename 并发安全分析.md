# 图书借阅并发安全分析

## 涉及源码

| 文件 | 方法 | 作用 |
|------|------|------|
| `BorrowService.java` | `borrowBook` | 借书核心逻辑：校验 → 写入 |
| `BorrowRecordMapper.java` | `countActiveBorrowsByBookId` | 统计某书当前在借记录数 |
| `BorrowRecordMapper.java` | `insert` | 插入一条借阅记录 |
| `BorrowRecordMapper.java` | `findBorrowedByUserIdAndBookId` | 查询用户对某书是否已有未还记录 |

---

## 一、并发时序交错分析

### 关键代码路径（`BorrowService.borrowBook`）

```
1. findBorrowedByUserIdAndBookId  → 幂等校验（普通 SELECT）
2. countActiveBorrowsByBookId     → 库存校验（普通 SELECT COUNT(*)）
3. insert                         → 写入记录（普通 INSERT）
```

三条 SQL 均为**不加锁的普通查询/写入**，且 `sys_borrow_record` 表上**没有任何唯一约束**。

### 交错时序

Alice（事务 T1）与 Bob（事务 T2）几乎同时调用 `borrowBook("alice", 5)` 和 `borrowBook("bob", 5)`：

| 时刻 | T1（Alice） | T2（Bob） |
|------|-------------|-----------|
| t1 | `BEGIN`，获取快照 S1 | |
| t2 | | `BEGIN`，获取快照 S2 |
| t3 | `countActiveBorrowsByBookId(5)` → **0** | |
| t4 | | `countActiveBorrowsByBookId(5)` → **0** |
| t5 | 校验通过（0 ≤ 0 不成立，不抛异常） | |
| t6 | | 校验通过 |
| t7 | `INSERT` (bookId=5, status='BORROWED') ✅ | |
| t8 | | `INSERT` (bookId=5, status='BORROWED') ✅ |
| t9 | `COMMIT` | |
| t10 | | `COMMIT` |

### 结果

`sys_borrow_record` 中会落下 **2 条** `bookId=5, status='BORROWED'` 的记录——同一本书同时被两个不同用户持有，**"单本馆藏"约束被打破**。

---

## 二、`@Transactional` 是否足以守住约束

**结论：不足以守住。**

### 原因分析

#### 1. REPEATABLE READ 的快照读无法感知并发写入

MySQL InnoDB 默认隔离级别为 REPEATABLE READ。该级别保证**同一事务内多次普通 SELECT（快照读）结果一致**，但快照读基于事务开始时的 MVCC 快照，**看不到其他事务尚未提交或已提交的新增行**。

在上述时序中：
- T1 在 t3 读取快照，看到 `COUNT(*) = 0`
- T2 在 t4 读取快照，同样看到 `COUNT(*) = 0`
- 两者互不可见对方的 INSERT，因为 INSERT 产生的新行不在对方的快照中

#### 2. "校验—写入"之间存在时间窗口（TOCTOU 漏洞）

`BorrowService.borrowBook` 的逻辑本质是经典的 **Check-Then-Act** 模式：

```
if (countActiveBorrowsByBookId(bookId) > 0)  ← Check（不加锁）
    throw ...
insert(record)                                 ← Act
```

Check 与 Act 之间存在时间窗口（t3→t7），在此窗口内另一事务可以完成相同的 Check 并通过校验。`@Transactional` 只保证方法内操作的原子性（要么全部提交、要么全部回滚），**并不保证 Check 与 Act 作为一个整体对其他事务不可见**。

#### 3. 无锁查询 + 无唯一约束 = 双重失守

| 防线 | 现状 | 后果 |
|------|------|------|
| 行锁 / 间隙锁 | `countActiveBorrowsByBookId` 是普通 `SELECT`，不获取任何锁 | 其他事务可自由插入同一 `book_id` 的新行 |
| 唯一约束 | `sys_borrow_record` 无唯一索引 | 数据库层面无法拒绝重复的 `(book_id, status='BORROWED')` 组合 |

即使将隔离级别提升到 SERIALIZABLE，若不配合锁机制或约束，仍可能出现问题（SERIALIZABLE 下普通 SELECT 会加共享锁，可能导致死锁，但并非可靠方案）。

---

## 三、修复思路

### 思路一：数据库唯一约束（推荐）

**原理**：让数据库成为最终守门人，从根源杜绝脏数据写入。

**实现方式**（MySQL 8.0+）：

```sql
-- 利用函数索引实现条件唯一：仅当 status='BORROWED' 时对 book_id 施加唯一约束
CREATE UNIQUE INDEX uk_book_borrowed
ON sys_borrow_record ((CASE WHEN status = 'BORROWED' THEN book_id ELSE NULL END));
```

> MySQL 5.7 及以下不支持函数索引，可改用额外列 `active_book_id`：
> - `status='BORROWED'` 时 `active_book_id = book_id`
> - `status='RETURNED'` 时 `active_book_id = NULL`
> - 对 `active_book_id` 建普通唯一索引（NULL 不参与唯一性校验）

**代码层**：在 `BorrowService.borrowBook` 中捕获 `DuplicateKeyException`，转为业务异常：

```java
try {
    borrowRecordMapper.insert(record);
} catch (DuplicateKeyException e) {
    throw new RuntimeException("该书已被借出，请等待归还");
}
```

| 优点 | 缺点 |
|------|------|
| 最可靠，数据库层面硬保证，不依赖应用层逻辑 | MySQL 8.0 以下需额外列变通 |
| 无需加锁，并发性能最优 | 需变更表结构（DDL） |
| 实现简单，代码改动量小 | 异常驱动的控制流不够优雅 |

### 思路二：悲观锁（SELECT ... FOR UPDATE）

**原理**：在库存校验时主动加排他锁，阻塞其他事务的并发写入。

**实现方式**：先锁定图书主表记录，再执行校验与写入。

1. 在 `BookMapper` 中新增：

```java
@Select("SELECT * FROM sys_book WHERE id = #{id} FOR UPDATE")
Book findByIdForUpdate(Long id);
```

2. 在 `BorrowService.borrowBook` 中，**在库存校验之前**先锁定图书行：

```java
@Transactional
public void borrowBook(String username, Long bookId) {
    User user = userMapper.findByUsername(username);
    if (user == null) {
        throw new RuntimeException("无法定位借阅人信息");
    }

    // 悲观锁：锁定图书行，序列化同一本书的借阅请求
    Book book = bookMapper.findByIdForUpdate(bookId);
    if (book == null) {
        throw new RuntimeException("图书不存在");
    }

    List<BorrowRecord> existing = borrowRecordMapper.findBorrowedByUserIdAndBookId(user.getId(), bookId);
    if (!existing.isEmpty()) {
        throw new RuntimeException("您已借阅该书且未归还，请先处理前序记录");
    }

    int activeBorrows = borrowRecordMapper.countActiveBorrowsByBookId(bookId);
    if (activeBorrows > 0) {
        throw new RuntimeException("该书已被借出，请等待归还");
    }

    BorrowRecord record = new BorrowRecord();
    record.setUserId(user.getId());
    record.setBookId(bookId);
    borrowRecordMapper.insert(record);
}
```

> ⚠️ **为何不直接对 `countActiveBorrowsByBookId` 加 `FOR UPDATE`？**
>
> 当 `sys_borrow_record` 中不存在 `bookId=5 AND status='BORROWED'` 的行时，`SELECT ... FOR UPDATE` 在 REPEATABLE READ 下仅获取间隙锁（Gap Lock）。间隙锁之间互相兼容，两个事务可同时持有同一间隙的锁，随后双方 INSERT 互相等待对方的间隙锁释放，**导致死锁**。锁定父表 `sys_book` 的确定行则不存在此问题。

| 优点 | 缺点 |
|------|------|
| 无需变更表结构 | 并发性能下降，同一本书的借阅请求被完全串行化 |
| 逻辑清晰，代码可控 | 需额外一次 SELECT FOR UPDATE 查询 |
| 适用于所有 MySQL 版本 | 锁粒度选择不当易引发死锁 |

### 两种思路对比

| 维度 | 唯一约束 | 悲观锁 |
|------|----------|--------|
| 可靠性 | ⭐⭐⭐ 数据库硬保证 | ⭐⭐⭐ 依赖锁的正确使用 |
| 并发性能 | ⭐⭐⭐ 无锁等待 | ⭐⭐ 同一书串行化 |
| 实现成本 | ⭐⭐ 需 DDL 变更 | ⭐⭐ 需新增 Mapper 方法 |
| 侵入性 | 低（仅异常处理） | 中（改写业务流程） |
| 死锁风险 | 无 | 有（锁粒度不当） |

**推荐**：优先采用**唯一约束**，它是防御并发写入最根本、最简洁的手段；悲观锁可作为无法变更表结构时的备选方案。两者也可组合使用（唯一约束兜底 + 悲观锁优化体验），实现纵深防御。
