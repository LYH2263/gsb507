# 图书借阅系统并发问题分析报告

## 一、问题概述

针对 [BorrowService.borrowBook](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L55-L81) 方法在高并发场景下的"单本馆藏"约束失效问题进行分析。

---

## 二、并发时序交错分析

### 2.1 关键代码路径

1. **库存校验**：[BorrowRecordMapper.countActiveBorrowsByBookId](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L39-L40) 执行普通 SELECT 查询
   ```sql
   SELECT COUNT(*) FROM sys_borrow_record WHERE book_id = #{bookId} AND status = 'BORROWED'
   ```

2. **写入操作**：[BorrowRecordMapper.insert](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L18-L19) 执行 INSERT

### 2.2 Alice 与 Bob 并发时序（同一毫秒发起请求）

| 时间点 | Alice 的事务 (T1) | Bob 的事务 (T2) | 说明 |
|--------|------------------|----------------|------|
| t1 | `BEGIN` | - | T1 开启事务 |
| t2 | - | `BEGIN` | T2 开启事务 |
| t3 | `countActiveBorrowsByBookId(5)` → 返回 `0` | - | T1 执行快照读，读到无借出记录 |
| t4 | - | `countActiveBorrowsByBookId(5)` → 返回 `0` | T2 也执行快照读，同样读到 0（RR 下各自持有 read view） |
| t5 | 检查通过，开始构造 BorrowRecord | 检查通过，开始构造 BorrowRecord | 两个事务都认为库存可用 |
| t6 | `insert(...)` → 写入一条记录（未提交） | - | T1 执行 INSERT，此时行锁被 T1 持有 |
| t7 | - | `insert(...)` → **等待** | T2 尝试 INSERT，需要获取意向排他锁/行锁，阻塞等待 |
| t8 | `COMMIT` | - | T1 提交，释放锁，记录对其他事务可见 |
| t9 | - | 获得锁，继续执行 INSERT → **写入第二条记录** | T2 的 INSERT 顺利执行（因为 SQL 层面没有唯一约束） |
| t10 | - | `COMMIT` | T2 提交 |

### 2.3 最终结果

`sys_borrow_record` 表中会落下 **2 条** `book_id = 5` 且 `status = 'BORROWED'` 的记录——即 Alice 和 Bob 都成功借到了同一本书，"单本馆藏"约束被打破。

---

## 三、@Transactional 为何守不住约束

[BorrowService.borrowBook](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L55-L81) 上的 `@Transactional` **不足以**保证"单本馆藏"，原因如下：

### 3.1 REPEATABLE READ 下的快照读（一致性非锁定读）

MySQL InnoDB 在 REPEATABLE READ 隔离级别下，普通的 `SELECT`（不带 `FOR UPDATE` / `LOCK IN SHARE MODE`）执行的是**快照读**（Snapshot Read）：
- 每个事务在第一次执行普通 SELECT 时会创建一个 Read View；
- 后续的 SELECT 都基于这个 Read View，看不到其他事务**已提交但在 Read View 创建之后**的修改；
- 快照读**不加任何锁**，不会阻塞其他事务的读或写。

因此 Alice 和 Bob 在 t3、t4 时刻各自的 `countActiveBorrowsByBookId` 查询，彼此完全看不到对方的存在，都得到了 `activeBorrows = 0` 的结果。

### 3.2 "校验—写入"之间的竞态窗口

从 `countActiveBorrowsByBookId` 校验通过到 `insert` 真正落盘，中间存在一个不可忽视的时间窗口：
```
校验 (SELECT COUNT) → 构造对象 → INSERT → COMMIT
```
在这个窗口中，其他并发事务同样可以完成校验并进入 INSERT。`@Transactional` 只保证了单个事务内的原子性，但**无法保证跨事务的"先查后写"逻辑的串行化**——这本质上是一个典型的 **Check-Then-Act 竞态条件**。

### 3.3 表上没有唯一约束兜底

题目设定 `sys_borrow_record` 表**没有任何唯一约束**，因此即使在数据库层面也无法阻止同一 `book_id` 存在多条 `status = 'BORROWED'` 的记录。当 t9 时刻 Bob 的 INSERT 被放行后，数据库层面没有任何机制拒绝这次写入。

### 3.4 总结

| 机制 | 作用 | 是否解决该问题 |
|------|------|---------------|
| `@Transactional` | 保证单事务内原子性、一致性 | ❌ 不解决跨事务竞态 |
| REPEATABLE READ | 防止不可重复读、脏读，部分防止幻读 | ❌ 快照读让并发校验互相不可见 |
| 普通 SELECT | 不加锁、性能高 | ❌ 不会阻塞其他并发事务 |
| 无唯一约束 | — | ❌ 最后一道防线缺失 |

---

## 四、修复思路与方案比较

### 方案一：悲观锁（SELECT ... FOR UPDATE）

**思路**：将库存校验查询改为**当前读**，显式加排他锁，串行化对同一 book_id 的校验操作。

**实现方式**：
1. 修改 [BorrowRecordMapper.countActiveBorrowsByBookId](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L39-L40) 对应的 SQL：
   ```sql
   SELECT COUNT(*) FROM sys_borrow_record 
   WHERE book_id = #{bookId} AND status = 'BORROWED' 
   FOR UPDATE
   ```
   由于 `FOR UPDATE` 需要锁住相关行，若要确保"尚无借出记录时"也能阻塞并发请求，需要在 `sys_book` 表（图书主表）上对该 book_id 的行加锁（比如先 `SELECT * FROM sys_book WHERE id = #{bookId} FOR UPDATE`），作为"锚点行"。

**优点**：
- 实现简单，代码改动量小；
- 强一致性保证，不会出错。

**缺点**：
- 并发度低，同一本书的所有借阅请求会被串行化；
- 需要注意死锁风险（多本书交叉借阅时）；
- 若锁的是图书主表行，对"查询该书信息"等读操作也会产生阻塞（若别人也用 FOR UPDATE）。

---

### 方案二：数据库唯一约束（乐观兜底）

**思路**：在数据库层增加唯一约束，利用数据库的唯一索引来拒绝重复的"在借"记录。

**实现方式**：
1. MySQL 不支持直接对带条件的唯一索引（Partial Unique Index 在 8.0+ 可通过函数索引实现），可以采用以下两种子方案：
   - **子方案 2a：字段加唯一约束**——在 `sys_borrow_record` 表中增加一个可空字段 `active_book_id`，当 `status = 'BORROWED'` 时该字段等于 `book_id`，否则为 `NULL`，然后对 `active_book_id` 建 `UNIQUE KEY`（MySQL 中 UNIQUE 索引允许多个 NULL）。
   - **子方案 2b：借助图书主表状态字段**——在 `sys_book` 表增加 `status` 字段（可用/已借出），借阅时通过 `UPDATE sys_book SET status = 'BORROWED' WHERE id = #{bookId} AND status = 'AVAILABLE'`，根据 affected rows 是否为 1 判断是否抢到，再插入记录（这其实是乐观锁的一种变体，CAS 思想）。

**优点**：
- 数据库层面兜底，最可靠；
- 子方案 2b 性能好，无长事务持锁问题；
- 不需要显式加锁，并发度高。

**缺点**：
- 需要表结构变更；
- 子方案 2a 增加字段有一定侵入性；
- 需要在应用层捕获 `DuplicateKeyException` 并转化为友好的业务异常提示。

---

### 方案三（补充）：乐观锁（版本号机制）

**思路**：在图书表上加 `version` 字段，借阅时带版本号更新，类似 CAS。

**SQL 示例**：
```sql
UPDATE sys_book SET status = 'BORROWED', version = version + 1 
WHERE id = #{bookId} AND status = 'AVAILABLE' AND version = #{version}
```
根据 affected rows 判断成功与否。

**取舍对比表**：

| 维度 | 悲观锁 (FOR UPDATE) | 唯一约束 + 状态字段 CAS | 乐观锁 (version) |
|------|---------------------|-------------------------|-------------------|
| 一致性强度 | 最强 | 强 | 强 |
| 并发性能 | 低（串行化） | 高 | 高 |
| 代码改造成本 | 低（改 SQL） | 中（改表+改逻辑） | 中（改表+改逻辑） |
| 死锁风险 | 有 | 无 | 无 |
| 适用场景 | 并发量低、冲突频繁 | 并发量高、冲突较少 | 并发量高、冲突较少 |

**推荐**：**方案二（子方案 2b：图书表状态字段 CAS）** 为最佳折中——既不需要显式加锁，又在数据库层面有可靠兜底，性能也最好。若追求最小改动、可快速修复，**方案一（FOR UPDATE）** 也可行，但需要注意在图书主表上加锁而不是在借阅记录表上（因为借阅记录为空时 FOR UPDATE 无行可锁）。

---

## 五、问题位置汇总

| 问题点 | 源文件 | 方法/位置 |
|--------|--------|-----------|
| 库存校验使用普通快照读，不加锁 | [BorrowService.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L70) | `borrowBook` 第70行 |
| 校验查询是普通 SELECT COUNT，无 FOR UPDATE | [BorrowRecordMapper.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L39-L40) | `countActiveBorrowsByBookId` |
| INSERT 语句无唯一约束冲突兜底 | [BorrowRecordMapper.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java#L18-L19) | `insert` |
| @Transactional 未配合显式锁策略，无法防跨事务竞态 | [BorrowService.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java#L55) | `borrowBook` 注解处 |
