# 图书借阅系统并发问题分析报告

## 一、问题背景

本系统采用 Spring Boot + MyBatis 架构，图书为"单本馆藏"模式（同一本书同一时间只允许一个用户持有）。核心借阅逻辑位于 `BorrowService.borrowBook`，该方法标注了 `@Transactional` 注解，数据库使用 MySQL InnoDB 引擎，默认隔离级别为 REPEATABLE READ。

---

## 二、并发时序交错分析

### 问题定位
- 源文件：`BorrowService.java`
- 方法：`borrowBook`
- 问题代码位置：第 69-80 行（库存校验 → 插入记录）

### 并发场景时序

假设 `alice` 和 `bob` 几乎同时对 `bookId = 5` 发起借阅请求，时序如下：

| 时间点 | Alice 的事务（T1） | Bob 的事务（T2） | 数据库状态 |
|--------|-------------------|-----------------|-----------|
| t1 | 开启事务 | 开启事务 | 无活跃借阅 |
| t2 | 执行 `countActiveBorrowsByBookId(5)` → 返回 0 | | 无活跃借阅 |
| t3 | | 执行 `countActiveBorrowsByBookId(5)` → 返回 0 | 无活跃借阅 |
| t4 | 校验通过，准备插入 | 校验通过，准备插入 | 无活跃借阅 |
| t5 | 执行 `insert`，写入 alice 的借阅记录 | | 1 条活跃借阅（alice） |
| t6 | | 执行 `insert`，写入 bob 的借阅记录 | 2 条活跃借阅（alice + bob） |
| t7 | 提交事务 | | 2 条活跃借阅 |
| t8 | | 提交事务 | **2 条活跃借阅** |

### 最终结果
`sys_borrow_record` 表中会落下 **2 条** 状态为 `BORROWED` 的借阅记录（分别属于 alice 和 bob），违反了"单本馆藏"约束。

---

## 三、@Transactional 为何不足以守住约束

### 问题定位
- 源文件：`BorrowService.java`
- 方法：`borrowBook`
- 注解位置：第 55 行 `@Transactional`

### 原因分析

**1. REPEATABLE READ 隔离级别的局限性**

MySQL InnoDB 的 REPEATABLE READ 隔离级别保证的是**事务内读一致性**（即同一事务内多次读取同一条数据结果一致），但它：
- 不会阻止其他事务插入新记录
- 不会对读取的数据加锁
- 存在"幻读"问题（虽然 InnoDB 在一定程度上通过间隙锁缓解了部分场景，但对于普通 SELECT 快照读并不生效）

**2. "校验—写入"时间窗口（TOCTOU 问题）**

`countActiveBorrowsByBookId` 是**快照读**（不加锁的普通查询），而 `insert` 是**当前读**。两者之间存在时间窗口：
- 校验时读到的是事务开始时的快照
- 写入时才真正操作最新数据
- 这个窗口就是并发漏洞所在

**3. 缺少数据库层面的约束**

`sys_borrow_record` 表上没有任何唯一约束，数据库本身不会阻止重复插入。

**结论：** `@Transactional` 只能保证方法内多个操作要么都成功要么都失败，但**无法防止并发事务之间的数据竞争**，不足以守住"单本馆藏"约束。

---

## 四、修复方案

### 方案一：数据库唯一约束（推荐）

**思路：** 在数据库层面建立唯一约束，从根本上阻止重复插入。

**实现方式：**

由于直接在 `(book_id, status)` 上建唯一索引不可行（同一本书可以有多条已归还记录），可以采用以下两种变体：

**变体 A：使用部分索引（MySQL 8.0+）**
```sql
CREATE UNIQUE INDEX uk_book_active 
ON sys_borrow_record(book_id) 
WHERE status = 'BORROWED';
```

**变体 B：增加辅助字段 + 普通唯一索引**
```sql
ALTER TABLE sys_borrow_record 
ADD COLUMN active_flag INT DEFAULT 1;

-- 借阅时 active_flag = book_id，归还时 SET NULL
CREATE UNIQUE INDEX uk_book_active ON sys_borrow_record(active_flag);
```

**对应代码修改：**
- 源文件：`BorrowRecordMapper.java`
- 方法：`insert` 和 `returnBook`
- 修改插入和归还 SQL，配合数据库约束

**优点：**
- 实现简单，性能最好
- 数据库层面保证，绝对可靠
- 不依赖应用层逻辑

**缺点：**
- 需要修改表结构
- 捕获唯一约束异常的业务代码不够优雅
- 部分索引需要 MySQL 8.0+

---

### 方案二：悲观锁（SELECT ... FOR UPDATE）

**思路：** 在库存校验时就对数据加排他锁，让并发事务排队等待。

**实现方式：**

修改 `countActiveBorrowsByBookId` 为加锁查询，或者新增一条加锁查询：

```java
// 方案：先锁住图书记录，再统计
@Select("SELECT * FROM sys_book WHERE id = #{bookId} FOR UPDATE")
Book lockBookById(Long bookId);
```

然后在 `borrowBook` 中先调用加锁查询：

```java
@Transactional
public void borrowBook(String username, Long bookId) {
    // 1. 先对图书记录加排他锁，阻塞并发事务
    Book book = bookMapper.lockBookById(bookId);
    if (book == null) {
        throw new RuntimeException("图书不存在");
    }
    
    // 2. 再进行库存校验（此时其他事务被阻塞，校验结果可靠）
    int activeBorrows = borrowRecordMapper.countActiveBorrowsByBookId(bookId);
    if (activeBorrows > 0) {
        throw new RuntimeException("该书已被借出，请等待归还");
    }
    
    // 3. 插入记录
    borrowRecordMapper.insert(record);
}
```

**问题定位：**
- 源文件：`BorrowService.java`
- 方法：`borrowBook`
- 修改位置：第 69-73 行之前增加加锁逻辑

**优点：**
- 逻辑直观，易于理解
- 适用于并发量不高的场景
- 不需要修改表结构

**缺点：**
- 性能较差，所有借阅同一本书的请求都要串行
- 容易造成死锁（如果加锁顺序不一致）
- 依赖 `sys_book` 表存在对应记录

---

### 方案三：乐观锁（可选补充方案）

**思路：** 在图书表上增加版本号或库存字段，更新时检查版本。

**实现方式：**
```sql
ALTER TABLE sys_book ADD COLUMN version INT DEFAULT 0;
```

```java
@Update("UPDATE sys_book SET version = version + 1 WHERE id = #{bookId} AND version = #{version}")
int updateBookVersion(Long bookId, Integer version);
```

**优点：** 高并发下性能好
**缺点：** 需要额外的重试机制，用户体验较差

---

## 五、方案对比与取舍

| 维度 | 数据库唯一约束 | 悲观锁（FOR UPDATE） | 乐观锁 |
|------|--------------|-------------------|--------|
| **可靠性** | ⭐⭐⭐⭐⭐ 最高 | ⭐⭐⭐⭐ 高 | ⭐⭐⭐ 中 |
| **性能** | ⭐⭐⭐⭐⭐ 最好 | ⭐⭐ 较差 | ⭐⭐⭐⭐ 好 |
| **实现复杂度** | ⭐⭐⭐ 简单 | ⭐⭐⭐ 简单 | ⭐⭐ 稍复杂 |
| **用户体验** | ⭐⭐⭐ 一般（异常提示） | ⭐⭐ 较差（等待超时） | ⭐⭐ 较差（需要重试） |
| **是否改表** | 是 | 否 | 是 |
| **适用场景** | 绝大多数场景 | 并发量低、写冲突少 | 读多写少、冲突概率低 |

### 推荐方案

**首选：数据库唯一约束 + 应用层异常捕获**

理由：
1. 数据库约束是最可靠的防线，不会因为应用层 Bug 失效
2. 性能最好，没有额外的锁等待开销
3. 实现简单，维护成本低

**组合使用建议：**

可以将"数据库唯一约束"作为**最终防线**，同时在应用层保留"库存校验"逻辑作为**前置拦截**——这样既能快速返回友好的错误提示（大部分并发请求被前置校验拦住），又能通过数据库约束保证绝对正确性。

---

## 六、涉及文件与方法汇总

| 问题点 | 文件 | 方法/位置 |
|--------|------|----------|
| 并发漏洞主逻辑 | [BorrowService.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java) | `borrowBook` 方法（第 55-81 行） |
| 不加锁的库存查询 | [BorrowRecordMapper.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java) | `countActiveBorrowsByBookId` 方法（第 39-40 行） |
| 插入操作 | [BorrowRecordMapper.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/mapper/BorrowRecordMapper.java) | `insert` 方法（第 18-19 行） |
| 事务注解不足 | [BorrowService.java](file:///d:/Agsb/gsb507/backend/src/main/java/com/longmao/demo/service/BorrowService.java) | 第 55 行 `@Transactional` |
